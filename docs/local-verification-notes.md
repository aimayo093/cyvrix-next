# Local verification notes

## Passed checks

- `npx tsc --noEmit`
- Focused ESLint checks for the trust controls, public cache, public service journeys, config and public-layout changes
- `prisma validate` using a non-routable local verification URL (no database connection)
- `npm run build` with outbound Sentry authentication disabled; TypeScript completed and all 91 pages generated, including the assessment directory and five static assessment routes plus 12 static service-detail routes.
- Local browser smoke tests for the homepage, Services, and all four enquiry routes. Each route retained its intended service selection.
- V3 browser checks for the six public service categories and their matching enquiry selections: Managed IT, Cloud, Cybersecurity, Infrastructure, Professional Services and Field Engineering.
- Browser and visual check for `/trust`: the Trust Centre renders without credential/logo placeholders and no browser errors.
- Responsive navigation check at 1,280px and 1,024px: the full V3 page set is available on desktop; the accessible tablet menu opens, routes correctly and closes after navigation.
- Focused TypeScript and ESLint checks for the assessment conversion routes, assessment payload handling, booking form and homepage entry points.
- Local browser checks for the assessment directory, all five dedicated assessment forms and the homepage assessment entry point. Each form exposes its selected assessment context, consent control and submit control without browser errors.
- Local browser checks for the Services directory confirmed all six V3 service categories, the 12 detailed service routes and evidence-safe static fallback content when the public CMS data is unavailable.
- Representative browser checks across Managed IT, Cloud, Cybersecurity, Infrastructure, Field Engineering and Professional Services confirmed each primary CTA carries the matching preselected enquiry service. The Cybersecurity assessment CTA was also verified.
- `prisma generate` completed against the expanded local schema using a non-routable placeholder `DATABASE_URL`; no database connection or migration was attempted.
- Local browser checks for `/pricing` confirmed the three productised managed IT plans, evidence-safe request-pricing treatment, specialist commercial routes and the managed-plan booking selection. No browser errors were reported.
- A subsequent optimised production build completed with the service-product CMS code; Next produced a fresh build ID and prerender manifest after releasing its build lock.
- Local browser checks for `/industries` confirmed all 10 static sector journeys remain available when public CMS data is unavailable. Healthcare correctly routes to Cybersecurity, while Construction and Field Teams routes to Field Engineering; the healthcare booking route was confirmed with its selected service and no browser errors.
- A subsequent optimised production build completed with the static industry routes and released its build lock after generating the new route output.
- Focused TypeScript and ESLint checks for the public Insights routes, CMS cache handling, sitemap and Insights admin page.
- Local browser checks for `/blog` and all three article routes confirmed their headings, useful article content and CTA routes. The Microsoft 365 article selects Cybersecurity, continuity selects Cloud & Cybersecurity, and managed IT selects Managed Services; no browser errors were reported.
- Local HTTP check for `/sitemap.xml` returned `200` and included the Insights index, a static Insight, Services and Industries routes.
- A final optimised production build completed after the Insights and sitemap update, wrote a fresh build ID and released its build lock.
- Focused TypeScript and ESLint checks for the public About, Careers and Contact routes.
- Local browser checks confirmed About renders its safe fallback content, Careers does not invent openings, and Contact exposes all six service interests when CMS services are unavailable. Contact no longer shows unconfigured email addresses or unsupported response-time wording; no browser errors were reported.
- A final optimised production build completed after the public-page resilience update, wrote build ID `skSogv__sxAQqS3sW7l4K` and released its build lock.
- Focused TypeScript and ESLint checks for the public Privacy, Terms, Cookie Policy, legal rendering helper and sitemap changes.
- Local browser checks for `/privacy-policy`, `/terms` and `/cookie-policy` confirmed the controlled publication state, no internal CMS wording and no unsupported cookie claim. Local HTTP validation confirmed the sitemap uses the three real legal URLs and no invalid `/legal/...` prefix.
- An optimised production build completed after the legal publication-gate update, wrote build ID `ck2pBAomAuNxEXkNitDID` and released its build lock.
- Focused TypeScript and ESLint checks for the public Support and FAQ routes. Local browser checks confirmed the Support client and new-enquiry routes, eight usable FAQ entries and the consultation CTA; no browser errors were reported.
- A final optimised production build completed after the Support and FAQ resilience update, wrote build ID `Tx_dVAV0iBVIeLck4fW3B` and released its build lock.
- Focused TypeScript and ESLint checks passed for the Legal Pages editor, legal route definitions, sitemap and updated admin navigation. The shared sidebar state-restoration hook was also brought into compliance with the focused React lint rule.
- Local browser checks confirmed `/privacy-policy`, `/terms` and `/cookie-policy` each retain the controlled publication state without internal CMS wording. `/admin/legal-pages` redirected to `/login?next=%2Fadmin%2Flegal-pages` as expected for an unauthenticated request.
- Local HTTP validation confirmed the sitemap includes the three canonical legal routes and no `/legal/...` path. A subsequent optimised production build completed with build ID `R2fWSfL-W1QIwddEBl7Qm`, included `/admin/legal-pages`, and released its build lock.
- Focused TypeScript and ESLint checks passed for the Contact CMS and public Contact route. A focused contact-settings guard test confirmed the legacy placeholder emails and telephone details are filtered, while an approved value is retained and unapproved fields are dropped.
- Local browser validation for `/contact` confirmed the safe enquiry fallback, no legacy contact details or SLA claims, and all six service interests. A final optimised production build completed with build ID `CXEdOFQYDiHJioJVUuRmS` and released its build lock.
- `git diff --check` for source changes, excluding regenerated Prisma output.
- Focused TypeScript and ESLint checks passed for the portal overview, portal modules, portal actions, portal layout, admin overview, and status-reporting controls. The client portal no longer shows hard-coded operational health, subscriptions, coverage, renewal dates, or response-time promises.
- Local browser checks confirmed unauthenticated requests to `/portal`, `/portal/services`, `/admin`, and `/admin/status` show the login screen rather than private data. The browser repeated the existing local Prisma `EACCES` fallback warning for favicon metadata only.
- A final optimised production build completed after the portal and dashboard credibility pass with build ID `GWP7Vi3AOW7_1-WvNHTfQ`. TypeScript completed and all 103 pages generated; the build exited successfully while retaining the existing sandbox database-fallback warnings.
- The portal navigation audit corrected the logout control to submit a `POST` request to the existing `/api/auth/logout` endpoint instead of linking to a non-existent `/logout` route. Focused TypeScript and ESLint checks passed, and an optimised production build completed with build ID `etiu3srUe14UfWEyxANK-`.
- The account-entry audit removed the unavailable `/forgot-password` link in favour of the working Contact support route. It also restricts post-login return navigation to recognised `/portal` and `/admin` paths, so an untrusted `next` query cannot be passed to `router.push`. Local browser validation confirmed the signed-out login interface and support link; focused TypeScript and ESLint checks passed, and an optimised production build completed with build ID `nCOiL0Kyk4m8ggdL7gfVm`.
- The Security Center now labels its result as a scoped check score rather than a security certification, identifies the exact technical checks it can perform, and makes the Vercel Cron, recipient, and email-transport prerequisites explicit. The manual-scan card no longer shows simulated percentage progress. Malformed stored scan metadata is ignored rather than presented as a valid result. Focused TypeScript and ESLint checks passed, and an optimised production build completed with build ID `wMizTHS4kh9PXdvEYHAFb`. No scan was triggered locally because it would create audit records and perform outbound monitoring requests.
- The quote, contact, ticket, and thank-you journeys now avoid unsupported CRM, scheduled-notification, response-time, and SLA promises. The quote form requires explicit sector and service selections, links to the Privacy Policy, and warns against secrets. Local browser checks covered `/request-quote` and `/thank-you?type=quote` without submitting data; focused TypeScript and ESLint checks passed, and an optimised production build completed with build ID `785cuGLxHP0Y0VvCVwer4`.
- The public CMS contact form now collects application interest and enquiry details without accepting attachments. The endpoint rejects any legacy multipart attachment before it reaches the lead-creation path; a local verification request returned the expected `303` refusal and did not create a lead. Careers fallback browser validation confirmed no file input, and an optimised production build completed with build ID `KUPAYdz2PSTXiPumoySdl`.
- The CMS image-upload endpoint now returns JSON `401`/`403` responses instead of redirecting API requests, permits only Super Admin, Admin and Content Manager roles, validates PNG/JPEG/WebP signatures rather than trusting browser MIME data, and requires a pre-provisioned `public-media` bucket plus a server-side service-role key. It no longer creates buckets or uses a local-disk fallback. Upload metadata and an audit event are written atomically; failed metadata storage requests bucket cleanup. Focused TypeScript and ESLint checks passed, a signed-out local POST returned `401`, and an optimised production build completed with build ID `iFNL86JI09KTjSRpHuA_X`.
- Public contact, CMS enquiry/application-interest and newsletter forms now require an explicit Privacy Policy acknowledgement. Contact submissions record a consent timestamp; newsletter confirmation emails include an HMAC-signed unsubscribe URL, `List-Unsubscribe` and RFC 8058 one-click headers, and `/api/unsubscribe` rejects missing or invalid tokens before looking up a subscriber. The unsubscribe endpoint uses a user-facing GET confirmation page and a blank `202` POST response for mail-client one-click actions. Newsletter records remain `pending` until Resend accepts the confirmation email; unavailable or failed delivery returns `503` instead of activating a subscriber. Production subscription also fails closed if either a signing secret or canonical site URL is unavailable. Browser checks confirmed the visible contact/footer controls and contact submit gating. Local missing-consent probes returned `422`; unsigned unsubscribe GET and POST requests returned `400`; a consented subscription with delivery deliberately unavailable returned `503`; focused TypeScript, ESLint and diff-integrity checks passed. An optimised production build completed with build ID `6nPvXgKauUKeCuCb_rx9I`.
- Public-site optional technologies are now gated by a visible, keyboard-accessible cookie-preferences control. Essential use remains available, while analytics, preference and marketing choices default to off and are saved in a versioned first-party `cyvrix_cookie_consent` cookie (`SameSite=Lax`, secure over HTTPS, one-year maximum age). Vercel Analytics is dynamically mounted only after analytics consent. The static CSP now adds `base-uri`, `form-action`, `frame-ancestors`, `frame-src`, `object-src`, `manifest-src` and `worker-src` restrictions; `unsafe-eval` is retained only in development as required by Next.js tooling. Browser checks confirmed the controls, optional-analytics gate and a clean console after rejecting optional categories. Local HTTP validation confirmed `DENY` clickjacking protection, HSTS, the expanded Permissions Policy, cross-domain-policy restriction and CSP directives. Focused TypeScript and ESLint checks passed, and an optimised production build completed with build ID `Qy_NjcUzw_RUqEJecEQbk`.
- Public submission and sign-in controls now use one bounded local rate-limit layer. Contact, quote, assessment, ticket, career and newsletter routes limit both source address and email address, returning `429`, `Retry-After` and `Cache-Control: no-store` when blocked. Login uses a 15-minute source-address limit plus a source-and-email limit, resets only after a successful sign-in, and does not impose a user-wide account lockout. The existing API middleware now bounds its source-address map and emits the same retry guidance. Focused TypeScript and ESLint checks passed; an optimised production build completed with build ID `W2XaSg8DUCz4BPMvbUrIN` and all 103 pages generated. The sandbox continued to deny build-time Prisma access (`EACCES`), so existing public fallbacks were used.
- The legacy navigation-management server actions now independently enforce an administrator session, validate labels, sort order and navigation URLs (internal path, anchor or HTTPS only), and write the create, update and delete decisions to the audit log in the same database transaction as the navigation change. Focused TypeScript and ESLint checks passed; an optimised production build completed with build ID `IQ_TZ-EzDBGWNaEzjD8wl` and all 103 pages generated. No navigation record was created or changed during local verification.
- The cron security-scan route was confirmed to fail closed without `CRON_SECRET`. Related satisfaction-survey delivery now defaults to disabled, requires an administrator to create a request, and only sends after both the mail key and canonical site URL are present; otherwise its request is recorded as `failed`, not `sent`. Ticket and work-order status changes require the relevant survey trigger to be explicitly enabled before invoking that workflow. Focused TypeScript and ESLint checks passed; an optimised production build completed with build ID `dOY6qtiB6bGhaAz6gixQ0` and all 103 pages generated. No scan, survey, email or database record was created during local verification.
- The public health endpoint now reports only availability and time, with `Cache-Control: no-store` and `X-Robots-Tag: noindex, nofollow`; it no longer exposes process uptime. The unused generic CMS API is now reserved for the Super Admin role, gives non-authorised callers a generic `403`, records the actor and source address in its audit entries, and uses controlled error responses. Focused TypeScript and ESLint checks passed; an optimised production build completed with build ID `27CFZpUcg-zmqWsbG6iFe` and all 103 pages generated. The existing sandbox Prisma `EACCES` fallbacks remained the only build-time data limitation.
- Manual Security Center scans and the Security Center settings page are now restricted to Super Admin and Admin roles. The scan API uses direct session checks and returns JSON `401` or `403`, instead of redirecting an API request; Content Manager, Sales, Support and Finance roles cannot obtain the page's server-action capability. Focused TypeScript and ESLint checks passed; an optimised production build completed with build ID `DvYJ-3MMx_5dIufS79W0p` and all 103 pages generated.
- System Settings is now Super Admin-only. The generic settings action validates setting keys, records the responsible user in its audit event, and allows a regular Admin to update only `securityCenter` configuration. The raw settings inspector deliberately excludes `emailConfig`, so a stored SMTP password is never rendered into the browser; the dedicated email form continues to mask it. Focused TypeScript and ESLint checks passed; an optimised production build completed with build ID `KfCQ8va4Ab4t8xlKjauVE` and all 103 pages generated.
- All runtime SMTP delivery now reads only server-managed `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER` and `SMTP_PASSWORD` credentials (with documented `MAIL_*` compatibility names). Stored `emailConfig` values are limited at runtime to the non-secret sender identity and notification inbox; public Resend flows, Security Center alerts and administrator broadcasts share that safe identity. System Settings no longer displays or accepts SMTP fields, and its approved form strips legacy credential properties on the next identity save. TypeScript passed and an optimised local production build completed with build ID `UlldIpIfFUEl4_lsS0DZ6`; the full focused ESLint invocation remains blocked only by pre-existing findings in large legacy files.
- Branded error pages and site search were added to close the two remaining gaps against sections 35 and 36. A shared `SiteMessageScreen` component now backs `app/not-found.tsx` (404), `app/error.tsx` (500), `app/forbidden.tsx` (403) and `app/maintenance/page.tsx`, so no error state exposes a stack trace, digest detail beyond a non-identifying reference, or internal diagnostics. `experimental.authInterrupts` was enabled because `forbidden.tsx` depends on it, and `requireSuperAdmin()` now calls `forbidden()` instead of silently redirecting an authenticated internal user to `/admin`.
- Site search covers Services, Industries, Insights, Case Studies and Careers through `lib/site-search.ts`. The index is built from published CMS content with the reviewed static catalogue as its fallback, so nothing withheld by the publication or Trust & Credentials gates can be surfaced by search. Two new cached readers, `getPublicCareerJobs()` and `getPublicCaseStudies()`, were added to `lib/public-cache.ts`; the case-study reader applies the full verified, evidenced, unexpired and permissioned gate in addition to `published`. The query is only ever used for in-memory comparison and is capped at 80 characters and eight terms; `/search` is marked `noindex` and is not in the sitemap.
- Local verification: `npx tsc --noEmit` passed, focused ESLint passed for every new file plus `lib/public-cache.ts`, `lib/auth.ts` and `components/nav-main/Footer.tsx`, and an optimised production build exited successfully with 105 pages and `/search`, `/maintenance` and `/_not-found` registered. A request to an unmatched route returned HTTP `404` with the branded page. Search was exercised over four queries: `microsoft 365` returned two services, two industries and one insight; `cybersecurity` returned one service, five industries and one insight; `healthcare` returned one industry; and an unmatched query rendered the no-results guidance and suggested searches. Search entry points were confirmed in the desktop utility bar, the mobile menu and the footer.
- The homepage was restructured around the four commercial engines and the competing service taxonomies were removed. The page previously presented the offering three times in succession: four engines, then six “core capabilities”, then six “supporting capabilities”. It now states one taxonomy. Each engine carries its engagement model (ongoing monthly service, consulting and specialist projects, contract and call-off delivery, fixed-scope projects) so a buyer can see how the work is scoped and bought, and the twelve detailed service routes are listed inside the engine they belong to rather than as a second list. Engine contents are resolved against the live service catalogue, so an unpublished service drops off the homepage instead of linking to a dead page. The engagement labels describe commercial structure only; no price, response time or service level is implied.
- The homepage title no longer repeats the brand. `generateMetadata` now returns an absolute title, because the root layout template appends “| CYVRIX Technologies” to a title that already carried it; the tab previously read “CYVRIX Technologies | Managed IT and Cybersecurity | CYVRIX Technologies”.
- The Pages CMS hero editor no longer pre-fills unsupported claims. Its stat-card fields defaulted to “Average Response Time”, “12”, “Mins” and “SLA Exceeded” as `defaultValue`, so an administrator saving the form would have written those figures to the database, where `SectionRenderer` can publish them. The defaults are now empty, the suggested examples no longer propose an ISO 27001 or operations-centre claim, badge visibility defaults to hidden in line with the public renderer's strict check, and the panel carries a visible evidence warning.
- Local verification: `npx tsc --noEmit` passed and focused ESLint passed for the homepage, its metadata and the Pages CMS editor. An optimised production build exited successfully; the prerendered homepage confirms the four engagement models, the removal of both competing capability lists, all twelve service detail links, and the corrected title. Browser validation against the production build confirmed the section order reads hero, engines, assessments, delivery model, call to action.
- The Services page now uses the same four-engine taxonomy as the homepage, and both read from one source. `lib/service-engines.ts` holds the engines, their engagement models and the detailed service routes each one contains; `resolveEngines()` filters against the live catalogue so an unpublished service disappears rather than linking to a dead page, and `findUnmappedServices()` surfaces any CMS service the mapping does not claim so new content is never silently dropped. `/services` presents one section per engine with its services nested beneath it, replacing the previous six category cards followed by a flat list of twelve.
- The duplicated brand in page titles was corrected across the site. The root layout appends “| CYVRIX Technologies” to every title, and 44 pages supplied a title that already carried the brand, so tabs and search results read “Technology Services | CYVRIX | CYVRIX Technologies”. Page titles now omit the brand; `app/admin/layout.tsx` and `app/portal/layout.tsx` define their own templates so internal tabs stay distinguishable as “… | CYVRIX Admin” and “… | CYVRIX Client Portal”. `stripBrandSuffix()` in `lib/utils.ts` removes a redundant separator-delimited brand suffix from CMS-supplied titles, because the template guarantees the brand is appended. It deliberately does not strip a bare trailing brand word, which would mangle a legitimate title such as “Why choose CYVRIX”.
- Three pages had no title and were inheriting the site default. `/case-studies` and `/login` are Client Components, so their metadata is declared in new pass-through layouts; `/thank-you` declares its own and is marked `noindex`. `/admin` and `/portal` index pages now carry their own titles rather than falling back to a layout default.
- Local verification: `npx tsc --noEmit` passed and focused ESLint passed for every changed file. An optimised production build exited successfully. Of 84 prerendered pages carrying a title, 83 now render the brand exactly once; the remaining page inherits the CMS value “About CYVRIX”, which is a content edit rather than a code defect.


## Existing follow-up items

- The stored SEO title for the About page is “About CYVRIX”, which renders as “About CYVRIX | CYVRIX Technologies”. Change it to “About” in **Admin → Pages** so the brand appears once. Review other stored `seoTitle` values for the same habit while the CMS is open.
- The header navigation still offers the previous category set. The homepage and Services page now share the four-engine taxonomy, but the header menu is database-driven, so aligning it belongs with the navigation records rather than in code.

- The live site at cyvrix.co.uk is still serving the previous build. It continues to display “Average Response Time 12 Mins”, “SLA Exceeded” and “Premium UK Operations Center”, which are exactly the claims this work removes. None of the credibility, structure or search work is deployed. A release is required before any of it takes effect publicly.
- The Services page and the header navigation still present six categories. The homepage is now the only surface using the four-engine taxonomy; aligning `/services` and the CMS-driven header menu is outstanding. The header menu is database-driven, so its restructure belongs with the navigation records rather than in code.

- The 403 path was not exercised end to end. Only a Super Admin account is seeded, and confirming it would require creating a lower-privileged user in the connected database, which this work deliberately did not do. In staging, sign in as an Admin, Content Manager or Sales user and confirm **Admin - System Settings** returns the branded 403 rather than a redirect.
- Adding two search links reused the existing `CustomLink` helper in `components/nav-main/Navbar.tsx`, which is declared inside the component body. This raises the pre-existing `react-hooks/static-components` count in that file from six to eight. The root cause is unchanged and should be fixed by hoisting `CustomLink` to module scope, using the correctly scoped `FooterNavLink` in `components/nav-main/Footer.tsx` as the reference shape.
- The full `npm run lint` command now runs under ESLint 9 but reports 4,601 legacy findings (1,909 errors and 2,692 warnings). The main categories are broad `any` use, older anchor patterns and generated/runtime files. They predate this focused upgrade and should be handled as a separately scoped lint-remediation effort.
- `npm install` reported 13 dependency advisories (1 low, 6 moderate, 6 high). No automated audit fix was applied.
- During static generation, this sandbox denies database access (`Prisma EACCES`). The application used its existing defensive fallbacks and the build exited successfully. A staging build with its approved database is still required before release.
- The published-only direct service lookup was type-, lint- and build-checked, but needs one staging CMS check with a draft service to confirm unpublished pages remain inaccessible when a database is available.
- The published-only industry lookup was type-, lint- and build-checked, but needs one staging CMS check with a draft industry to confirm unpublished URLs remain inaccessible when a database is available.
- The published-only Insight lookup respects both `PUBLISHED` status and `publishAt`. A staging CMS check should confirm a draft and a future-scheduled Insight are absent from the listing, direct URL and sitemap, and that cache invalidation makes a changed published article visible.
- Before release, configure and approve any public sales email, support email, telephone number and address in Contact settings. The editor now accepts only these six public fields; it removes legacy SLA/certification copy on the next save, while the public page filters the known legacy defaults immediately.
- Before release, publish reviewed legal content for Privacy, Terms and Cookie Policy through the `LegalPage` records. The public routes deliberately show a controlled publication notice until a substantive reviewed document is marked `PUBLISHED`; see `docs/legal-content-publication.md`.
- Before release, have the Cookie Policy reviewed to describe the first-party consent cookie and any enabled analytics, preference or marketing technologies accurately. In staging, repeat the consent check in a clean browser profile and confirm analytics traffic appears only after explicit analytics consent.
- Before release, sign in as an administrator in staging and verify saving a draft, rejecting an unconfirmed publication, then publishing one legally reviewed document through **Admin → Legal Pages**. This local run deliberately did not create or modify legal records.
- The Service Products CMS requires the prepared `20260815000000_service_products_cms` migration in an approved staging database before its editor can read or write records. Until then, the public pricing route uses its safe static fallback.
- Assessment forms were intentionally not submitted during local browser checks because a submission would create a real CRM lead. Their server-side schema and payload path are type-checked, but a staging database should be used for an end-to-end submission test before release.
- Public forms no longer accept attachments or persist CVs beneath `public/uploads`. Before enabling production document collection, connect approved private object storage, retention controls and authorised staff-only download access, then complete a staging upload and access-control test.
- Before release, provision and approve the existing `public-media` storage bucket for site imagery only, set `SUPABASE_SERVICE_ROLE_KEY` only in the server environment, and verify in staging that a Content Manager can upload a valid image while Support, Sales and Finance roles receive `403`. Confirm both the `MediaAsset` and `AuditLog` records, and that storage cleanup is requested if metadata recording fails.
- Before release, set a dedicated server-only `UNSUBSCRIBE_SECRET` (the implementation can fall back to the existing auth secret, but a separate secret is preferable), set the canonical `NEXT_PUBLIC_SITE_URL`, then complete one consented newsletter subscription and one signed unsubscribe in staging. Confirm the delivered Resend message contains the correct public URL and that the subscriber becomes unsubscribed only through that signed link.
- Before release, connect approved entitlement, renewal, and monitoring data sources if CYVRIX wants to report them in the portal. Until then, the portal and System Status area intentionally show no inferred service-health or availability information.
- Before release, test an authenticated staging portal account with its approved organisation records. The local checks deliberately did not sign in or submit a testimonial; portal feedback now stays private and audit-logged until the existing Trust & Credentials review, evidence, permission, and visibility controls approve it.
- Before release, configure a durable shared rate limiter at the deployment edge or through an approved shared store. The local in-memory controls are bounded and protect an individual runtime instance, but do not provide a global limit across horizontally scaled instances. In staging, repeat the contact, newsletter and unsuccessful-login flows to confirm `429` responses and `Retry-After` behaviour without creating production records.
- Before enabling automatic satisfaction surveys, configure and approve the survey settings, a canonical `NEXT_PUBLIC_SITE_URL` (or production Vercel URL), `RESEND_API_KEY` and sender domain. In staging, complete one explicitly enabled closed-ticket or completed-work-order survey journey and verify the request, audit event, delivery state and signed response link.
- Before release, provision the approved server-side SMTP secrets (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, or the documented compatible `MAIL_*` names) and complete an authenticated staging broadcast plus a Security Center alert delivery. After the environment delivery is confirmed, remove legacy SMTP credential properties from the existing `emailConfig` database record. This local work deliberately did not read, change or migrate any database record.
- Next warns that the legacy `middleware` convention should move to `proxy`; Sentry warns about a future client-config location and logger option update. Neither blocks the current build.
- The repository-wide `git diff --check` currently reports trailing-whitespace noise across many pre-existing dirty files because of line-ending normalization in the worktree. The SMTP pass uses a scoped whitespace check instead; no unrelated file was reformatted or changed for that issue.
- A repeat local Next build after the final recipient-normalisation adjustment generated build ID `GpCAqdQ75wQaebdmRU1pT` and completed the compiler artifact, but its worker process did not exit within the bounded local observation window. TypeScript and the focused ESLint checks passed after that change; repeat the build in clean staging/CI for an exit-status confirmation before release.

## Guardrails honoured

No production deployment, migration, database write, external publication, push, commit or account change was performed.
